Extraordinarly simple IRC client, inspired by LimeChat.

To use it you'll have to pull the npm package `irc`:

```
$ npm install irc
```

Then node-run `irc.js` as usual with the necessary Node-Five flags.
